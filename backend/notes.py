# passlib[bcrypt] -> it is basically used for secure password hasing
#passlib -> pass hashing library in fastapi
#bcypt -> secure hashingn algorithm for secure passwords
#-> it multiplies round of hashing which prevents brute-force attacks


#python-jose[cryptography] -> JWT authentication (JWT-> JSON web token) validates the credentials
# python-jose is used for JWT (JSON Web Token) authentication.
# [cryptography] enables a secure cryptographic backend for signing JWTs.
#Used for user login, API security, and OAuth2 authentication. OAuth2 -> Open authorization allow web ap[plication to access resources from other websites or apps


#python-multipart -> used for handling form data
# handling form data and file uploads

#FastAPI modules → APIRouter, HTTPException, Depends (for creating routes and handling errors).